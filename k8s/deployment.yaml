apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgresql-metadata
spec:
  selector:
    matchLabels:
      app: postgresql-label
  template:
    metadata:
      labels:
        app: postgresql-template
    spec:
      containers:
        - name: postgresql-container
          image: postgres:16.2-alpine3.19
          ports:
            - containerPort: 5432
          env: 
            - name: POSTGRES_PASSWORD
              value: "1234"
            - name: POSTGRES_USER
              value: "kubenews"
            - name: POSTGRES_DB
              value: "kubenews_db"
---
apiVersion: v1
kind: Service
metadata:
  name: postgresql-service
spec:
  selector:
    app: postgresql-selector
  ports:
  - port: 5432
    targetPort: 5432

